import type {Player, PlayerData, PlayerParams} from './Player';
import type {Match, MatchData, MatchParams} from './Match';

export type {
  Player,
  PlayerData,
  PlayerParams,
  Match,
  MatchData,
  MatchParams,
}
